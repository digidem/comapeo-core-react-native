import { Stack } from 'expo-router';

// Per-project nested stack. Header chrome (back button, title) is provided by
// each individual screen via <Stack.Screen options>.
export default function ProjectLayout() {
  return <Stack />;
}

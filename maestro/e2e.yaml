appId: com.comapeo.core.e2e
env:
    TIMEOUT: ${TIMEOUT || 10000}
---
- launchApp:
    clearState: true
- startRecording:
    path: "maestro/recordings/e2e"
- tapOn: "Run tests"
- extendedWaitUntil:
    visible: 
        id: "all-tests-passed"
    timeout: ${TIMEOUT}
